<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aleexalvz.pokedex">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_principal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_principal_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokeDex" >

        <activity
            android:name="com.aleexalvz.pokedex.apresentation.pokelist.PokeListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>